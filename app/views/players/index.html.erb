<h1>Choose Player</h1>

<table>
  <tbody>
    <% @players.each do |player| %>
      <tr>
        <td><%= player.name %></td>
        <td><%= link_to 'Select', player, method: :post, remote: true %></td>
      </tr>
    <% end %>
  </tbody>
</table>

